AppBundle\Entity\User:
    exclusion_policy: none
    properties:
        id:
            groups: ['users', 'posts', 'auth_tokens', 'comments']
        firstname:
            groups: ['users']
        lastname:
            groups: ['users']
        email:
            groups: ['users', 'posts', 'comments']
        password:
            groups: ['users']
        